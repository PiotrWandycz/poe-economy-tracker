# Scrape poe.ninja instead of using an API

poe.ninja does not expose a public API. Economy data is fetched by scraping poe.ninja pages from the .NET backend and caching the results server-side. This is the only available approach for obtaining the data.
